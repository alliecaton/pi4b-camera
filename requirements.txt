# Raspberry Pi Camera Project Requirements
# Install with: pip install -r requirements.txt

# Core camera library for Raspberry Pi
picamera2[gui]>=0.3.12

# Computer vision library
opencv-python>=4.5.0

# Numerical computing (dependency of opencv and picamera2)
numpy>=1.21.0

# Image processing library (optional, useful for advanced image manipulation)
Pillow>=8.0.0

# GPIO control (optional, useful if you want to add hardware controls)
RPi.GPIO>=0.7.0

# Alternative GPIO library (choose one: RPi.GPIO or gpiozero)
# gpiozero>=1.6.0